# WebTunnel thesis -- pinned to versions that ACTUALLY resolve on PyPI.
# F-17: the previous file pinned torch>=2.13.0, xgboost>=3.4.1, scikit-learn>=1.9.0,
# numpy>=2.5.0, pandas>=3.0.0, matplotlib>=3.11.0, shap>=0.52.0 -- none of which exist, so
# `pip install -r requirements.txt` could never succeed. CLAUDE.md pointed at a
# requirements-working.txt that was not in the repository either.
#
#   python3 -m venv venv && venv/bin/pip install -U pip && venv/bin/pip install -r requirements.txt
#
# For CUDA builds of torch, install torch separately from the PyTorch index first:
#   venv/bin/pip install torch --index-url https://download.pytorch.org/whl/cu124

numpy>=1.26,<3
scipy>=1.11,<2
pandas>=2.1,<3
scikit-learn>=1.3,<2
xgboost>=2.0,<4
torch>=2.1
dpkt>=1.9.8
matplotlib>=3.8,<4
seaborn>=0.13,<0.15
shap>=0.44,<1
tqdm>=4.66
joblib>=1.3
